# BMAM 正确的测试流程

## 修复总结

### ✅ 已完成
1. **任务类型感知配置** (NOT数据集感知,不作弊!)
   - 纯粹基于查询特征推断任务类型
   - temporal_reasoning, preference_query, identity_recall
   - 不识别"locomo", "prefeval"等数据集名称

2. **FAISS/SQLite写入顺序修复**
   - 先DB后FAISS,避免幽灵记忆

3. **UserPortraitManager**
   - 用户肖像持久化

---

## 正确的测试命令

### LoCoMo测试 (BMAM方式)

```bash
cd /Users/liyang/Desktop/testversion/BMAM/evaluation/benchmarks/locomo

# 抽样测试 (2组,每组20题)
python3 test_sequential.py --groups 2 --questions 20

# 全量测试 (10组,每组全部题目)
python3 test_sequential.py
```

### 其他数据集测试

```bash
cd /Users/liyang/Desktop/testversion/BMAM/evaluation/benchmarks

# LongMemEval
cd longmemeval
python3 test_longmemeval.py --max-samples 20

# PrefEval
cd prefeval
python3 test_prefeval.py --max-samples 20

# PersonaMem
cd personamem
python3 test_personamem.py --max-samples 20
```

---

## 与MemOS对比方式

MemOS使用的是分阶段评估流程:

```bash
cd /Users/liyang/Desktop/testversion/archived/new/MemOS/evaluation

# 1. 运行ingestion (摄入记忆)
# 2. 运行search (检索)
# 3. 运行responses (生成回答)
# 4. 运行eval (评估)

./scripts/run_locomo_eval.sh
./scripts/run_lme_eval.sh
./scripts/run_prefeval_eval.sh
./scripts/run_pm_eval.sh
```

**关键**:
- MemOS的评估流程和BMAM不同
- 需要确保使用**相同的数据集文件**
- 评估指标计算方式应该一致

---

## 配置系统说明

### ❌ 旧设计 (作弊)
```python
# 识别数据集名称
if dataset == "locomo":
    config = V1_config  # 针对LoCoMo优化
```

### ✅ 新设计 (不作弊)
```python
# 仅基于查询特征
if has_temporal_keywords(query):
    config = temporal_reasoning_config
elif has_preference_keywords(query):
    config = preference_query_config
```

**特征检测**:
- 时间推理: "when", "after", "before", "timeline"
- 偏好查询: "prefer", "like better", "which do"
- 身份回忆: "what is my", "do i like", "my favorite"

---

## 期望结果

| 数据集 | 历史最佳 | 期望 |
|--------|---------|------|
| LoCoMo | 75.88% | ≥70% |
| LongMemEval | 86.67% | ≥80% |
| PrefEval | 60% | ≥55% |
| PersonaMem | 52% | ≥50% |

**关键验证**:
1. 日志显示任务类型切换: `🎯 Task config switched: Default Task → Temporal Reasoning`
2. 无幽灵记忆错误
3. 生成`data/user_portraits.json`

---

## 下一步分析

### 为什么V2/V3特性会干扰V1?

需要分析的问题:
1. **偏好检索为何干扰时间推理?**
   - 偏好向量是否污染时间线检索?
   - persona_k=20是否检索噪声?

2. **evaluation_mode_boost副作用?**
   - 是否过度增强导致精度下降?

3. **user_id隔离问题?**
   - LoCoMo是单用户对话,隔离是否有意义?

建议:
- 运行消融实验,逐个关闭特性看影响
- 对比V1备份和当前版本的检索结果
- 分析误答case找根本原因

---

*更新时间: 2025-12-25*
